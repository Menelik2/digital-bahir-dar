# Frontend-safe only (Vercel Environment Variables / .env.local)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-browser-key

# Google Cloud: enable Maps JavaScript API
# Restrict browser key by HTTP referrer in production

# Edge Function secrets (supabase secrets set) — NEVER prefix with VITE_
# AI_API_KEY=
# AI_MODEL=gpt-4o-mini
